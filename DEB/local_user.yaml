---
- name: Create Local User
  hosts: all
  become: true
  gather_facts: no

  tasks:
    - name: Create User
      ansible.builtin.user:
        name: "{{ username }}"
        state: present
        shell: /bin/bash
        groups: "{{ super_group }}"
        append: yes
