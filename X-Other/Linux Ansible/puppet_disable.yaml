---
- hosts: all
  become: yes
  become_method: sudo
  gather_facts: false

   
  tasks:
  - name: Stop Puppet and Disable Service if exists
    service:
      name: puppet
      state: stopped
      enabled: no
    ignore_errors: yes
